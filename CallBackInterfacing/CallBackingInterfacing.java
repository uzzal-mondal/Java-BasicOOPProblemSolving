package OOPBest.CallBackInterfacing;

public interface CallBackingInterfacing {

    // that's create a interface with a signature don't use to body.
    public void CallInterfacingf(String data);
}
